{#
    * Need one array of dictionaries    : pages
    * Each dictionary should contain three keys and one optional key
            name        : Name to be displayed on the link
            link        : Relative link to the page
            active      : Boolean indicating whether this is the current page
            sub_links   : Optional | An array on links having the same three variables
#}
